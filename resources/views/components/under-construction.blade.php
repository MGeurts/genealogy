<div class="mt-5 flex flex-col items-center">
    <div>
        <img
            src="{{ asset('img/under-construction.webp') }}"
            alt="{{ __('app.under_construction') }}"
            class="w-xs rounded-sm"
        />
    </div>
    <div class="mt-2 text-lg text-red-500">{{ strtoupper(__('app.under_construction')) }}</div>
</div>
